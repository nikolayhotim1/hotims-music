import React from 'react';
import MainLayout from '../../layouts/MainLayout';

const create = () => {
    return (
        <MainLayout>
            <h1>Loading a track</h1>
        </MainLayout>
    );
};

export default create;